# Phase 21 — Wait status line: seam wiring & composition root

*Realizes design Decision 13 (the driver-side half: the `Waiter` seam, the driver's `Start`/`Stop` calls, and the TTY-gated bind), the Decision 1 `Waiter` seam-table row (no ids), and the Decision 5 driver wiring. Depends on Phase 20 (the `LiveWaiter` it binds), Phase 7b (the turn driver it extends), and Phase 8 (the composition root it wires).*

Wire the status line into the live path across `internal/repl` and `cmd/agentrepl/main.go`. End state: `repl` declares the `Waiter interface { Start(model string); Stop() }` and a `nopWaiter` (both methods empty); `Deps` gains a `Waiter` field; the turn driver calls `waiter.Start(conv.Model)` immediately before ranging the stream and `waiter.Stop()` both on the first event drawn and via `defer` (so success, error, empty, and interrupt all erase) — and runs the waiter **only while the decorated renderer is active**, so a turn taken after `/render raw` and any non-TTY run use the `nopWaiter`. The composition root binds a `*render.LiveWaiter` (writing to `IO.Out`, gray when `color`) into `Deps.Waiter` only when `IO.IsTTY`, and the `nopWaiter` otherwise. Tests drive the loop with a recording fake `Waiter` to assert the Start/Stop lifecycle and the raw/non-TTY nop.

**Done when:** R-6F74-SX99 (the driver calls `Start(conv.Model)` before draining the stream and `Stop()` on first output and via `defer` across success/error/empty/interrupt; raw and non-TTY use `nopWaiter` so no status line appears in their output) is covered by clearly-named repl-level tests (recording fake waiter) and the suite is green.
